export class Constants {
    // Local storage variables
    public static LOCAL_STORAGE_KEY_FILTERS: string = Object.freeze("Filters"); 

    // DOM query selectors
    public static WH_VAR_MENU_CONTAINER_SELECTOR: string = Object.freeze("#productlist_left_ultrawide");

}